[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "magicsandbox-streaming"
version = "0.1.0"
description = "Streaming utilities for Magic Sandbox"
readme = "README.md"
requires-python = ">=3.7"
license = {text = "MIT"}

[project.urls]
Homepage = "https://github.com/magicsandbox-ai/magicsandbox"