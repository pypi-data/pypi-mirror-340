[project]
name = "torchabc"
version = "0.2.1"
description = "An abstract class for training, evaluation, and inference of pytorch models."
authors = [
    {name = "Emanuele Guidotti",email = "emanuele.guidotti@usi.ch"}
]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "torch (>=2.6.0,<3.0.0)"
]

[tool.poetry.scripts]
torchabc = "torchabc.cli:main"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
