[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "colorona"
version = "0.2.6"
description = "A way to COMPLEATLY transform the way you look and feel about your terminal"
readme = "README.md"
requires-python = ">=3.6"
license = {text = "MIT"}
authors = [
  { name="Your Name", email="you@example.com" }
]
dependencies = []

