[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ajishCalc"
version = "0.1.0"
description = "Percentage calculator"
authors = [{ name = "Ajish", email = "you@example.com" }]
readme = "README.md"
requires-python = ">=3.7"
