[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/notify"]

[project]
name = "r00notify"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pyTelegramBotAPI",
]

