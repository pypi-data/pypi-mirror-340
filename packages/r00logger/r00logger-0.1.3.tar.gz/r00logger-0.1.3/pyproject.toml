[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/logger"]

[project]
name = "r00logger"
version = "0.1.3"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv",
    "loguru",
]

