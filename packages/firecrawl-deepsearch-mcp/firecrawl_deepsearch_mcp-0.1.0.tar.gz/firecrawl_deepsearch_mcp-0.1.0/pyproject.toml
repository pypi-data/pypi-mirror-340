[project]
name = "firecrawl-deepsearch-mcp"
version = "0.1.0"
description = "Deepsearch by firecrawl. MCP server"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [ "mcp>=1.6.0",]
[[project.authors]]
name = "Tolik"
email = "tolik@svl.services"

[build-system]
requires = [ "hatchling",]
build-backend = "hatchling.build"

[project.scripts]
firecrawl-deepsearch-mcp = "firecrawl_deepsearch_mcp:main"
