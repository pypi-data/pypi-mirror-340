[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "my-awesome-test-client-to-remove"
version = "0.0.1"
readme = "README.md"

dependencies = [
]
