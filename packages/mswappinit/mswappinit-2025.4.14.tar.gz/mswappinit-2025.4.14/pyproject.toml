[project]
name = "mswappinit"
version = "2025.04.14"
description = "a personal, opinionated python app initializer"
readme = "README.md"
authors = [{ name = "matt wartell", email = "matt.wartel@gmail.com" }]
requires-python = ">=3.12"
dependencies = ["loguru>=0.7.3", "pickledb>=1.3.2", "python-dotenv>=1.1.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = ["pytest>=8.3.5"]
