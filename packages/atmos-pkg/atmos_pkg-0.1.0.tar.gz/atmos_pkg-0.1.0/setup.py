"""Setup script for the atmos package."""

from setuptools import setup

# This setup.py file uses the configuration in setup.cfg
# but we also specify the name explicitly to ensure consistency
setup(
    name="atmos-pkg",
    # Other configuration is in setup.cfg
)
