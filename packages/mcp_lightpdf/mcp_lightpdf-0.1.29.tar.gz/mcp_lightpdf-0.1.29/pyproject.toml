[project]
name = "mcp_lightpdf"
version = "0.1.29"
description = "LightPDF AI助手 MCP Server"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp",
    "httpx",
    "python-dotenv",
]

[project.scripts]
mcp_lightpdf = "mcp_lightpdf:main"
