[project]
name = "saiesgreet"
version = "0.1.0"
description = "A tiny Python package to greet people."
authors = [
    { name = "Tejeswini", email = "pathivada142@gmail.com" }
]
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.7"
[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
