[project]
name = "lunarscan"
version = "0.1.0"
description = "A simple networking and port scanning module."
authors = [
    { name="64bit-lunarteam", email="64bitlunarteam@gmail.com" }
]
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.7"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project.urls]
Homepage = "https://github.com/64bit-lunarteam/LunarScan/"