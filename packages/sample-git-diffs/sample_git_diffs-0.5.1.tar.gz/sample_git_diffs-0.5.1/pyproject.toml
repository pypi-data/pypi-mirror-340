[tool.poetry]
name = "sample-git-diffs"
version = "0.5.1"
description = ""
authors = ["ninpnin <you@example.com>"]
packages = [{include = "sample_git_diffs"}]
license = "MIT"
readme = "README.md"
repository = "https://github.com/ninpnin/sample-git-diffs"

[tool.poetry.dependencies]
python = "^3.8"
pandas = "*"

[tool.poetry.scripts]
sample-git-diffs = "sample_git_diffs.sample_git_diffs:cli"
diff2markdown = "sample_git_diffs.to_md:cli"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
