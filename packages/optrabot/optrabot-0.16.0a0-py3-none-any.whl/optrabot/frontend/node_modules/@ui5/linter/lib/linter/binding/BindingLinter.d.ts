import LinterContext, { PositionInfo, ResourcePath } from "../LinterContext.js";
import { RequireDeclaration } from "../xmlTemplate/Parser.js";
export default class BindingLinter {
    #private;
    constructor(resourcePath: ResourcePath, context: LinterContext);
    lintPropertyBinding(bindingDefinition: string, requireDeclarations: RequireDeclaration[], position: PositionInfo): void;
    lintAggregationBinding(bindingDefinition: string, requireDeclarations: RequireDeclaration[], position: PositionInfo): void;
    lintPropertyExpression(bindingDefinition: string, requireDeclarations: RequireDeclaration[], position: PositionInfo): void;
}
