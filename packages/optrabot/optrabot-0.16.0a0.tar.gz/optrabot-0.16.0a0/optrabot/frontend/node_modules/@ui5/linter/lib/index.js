import { lintProject } from "./linter/linter.js";
export async function ui5lint(options) {
    const { filePatterns, ignorePatterns = [], details = false, config, noConfig, coverage = false, ui5Config, rootDir = process.cwd(), } = options ?? {};
    return lintProject({
        rootDir,
        filePatterns,
        ignorePatterns,
        coverage,
        details,
        configPath: config,
        noConfig,
        ui5Config,
    });
}
//# sourceMappingURL=index.js.map