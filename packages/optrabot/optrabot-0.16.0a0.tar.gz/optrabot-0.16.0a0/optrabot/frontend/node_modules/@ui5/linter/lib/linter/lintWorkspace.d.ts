import { AbstractAdapter } from "@ui5/fs";
import { LintResult, LinterOptions, FSToVirtualPathOptions } from "./LinterContext.js";
import { UI5LintConfigType } from "../utils/ConfigManager.js";
export default function lintWorkspace(workspace: AbstractAdapter, filePathsWorkspace: AbstractAdapter, options: LinterOptions & FSToVirtualPathOptions, config: UI5LintConfigType, patternsMatch: Set<string>): Promise<LintResult[]>;
