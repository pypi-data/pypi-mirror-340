import type { ReadStream } from "node:fs";
import { Detail, Reader, SaxEventType, Tag, Text } from "sax-wasm";
export declare function isSaxParserToJSON(tag: unknown): tag is Tag;
export declare function isSaxText(tag: unknown): tag is Text;
export declare function parseXML(contentStream: ReadStream, parseHandler: (type: SaxEventType, tag: Reader<Detail>) => void): Promise<void>;
