[project]
name = "otaku_media_info"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "JLoeve", email = "34429322+LonelySteve@users.noreply.github.com" }
]
requires-python = ">=3.8"
dependencies = []

[project.scripts]
start = "otaku_media_info:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build]
include = [
    "src",
    "README.md",
    "pyproject.toml",
]