[project]
name = "djt-mcp-server"
version = "0.1.1"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "duanjiatao", email = "duanjiatao@xgd.com" }
]
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.12",
]

[project.scripts]
djt-mcp-server = "djt_mcp_server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
