[project]
name = "vigentools"
version = "0.0.1"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115.12",
    "jwt>=1.3.1",
    "viconfig>=0.0.2",
    "vidbcontrol>=0.1.0",
    "vierror>=0.0.2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
