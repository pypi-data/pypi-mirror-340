[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "signalwire-pom"
version = "0.1.8"
description = "Prompt Object Model - A structured data format for organizing and rendering LLM prompts"
readme = "README.md"
authors = [
    {name = "SignalWire", email = "support@signalwire.com"}
]
license = {text = "MIT"}
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
requires-python = ">=3.6"
dependencies = [
    "docopt",
    "pyyaml",
]

[project.urls]
Homepage = "https://github.com/signalwire/signalwire-pom"
Issues = "https://github.com/signalwire/signalwire-pom/issues"

[project.scripts]
pom_tool = "examples.pom_tool:main" 