[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/trade_utils"]

[project]
name = "r00trade_utils"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [

]
